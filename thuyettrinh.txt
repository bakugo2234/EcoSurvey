================================================================================
  EcoSurvey — BÀI THUYẾT TRÌNH BẢO VỆ ĐỒ ÁN
  (Kịch bản trình bày chi tiết — từ tổng quan đến demo chuyên sâu)
================================================================================


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 1: TITLE — GIỚI THIỆU ĐỀ TÀI                               │
└─────────────────────────────────────────────────────────────────────┘

"Kính chào quý thầy cô và các bạn. Hôm nay nhóm em xin trình bày đồ án
tốt nghiệp với đề tài: EcoSurvey — Nền tảng Khảo sát Nhận thức Môi trường
và Gamification trong Học đường.

Đề tài giải quyết bài toán thực tế: Làm thế nào để thu thập ý kiến của
sinh viên và nhân viên về các vấn đề môi trường một cách có hệ thống,
đồng thời khuyến khích họ tham gia các hoạt động xanh thông qua cơ chế
tích điểm và xếp hạng — biến hành động bảo vệ môi trường thành một
trải nghiệm tương tác và có tính cạnh tranh."


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 2: GIỚI THIỆU NHÓM & PHÂN CÔNG                              │
└─────────────────────────────────────────────────────────────────────┘

"Nhóm phát triển gồm các thành viên:

  • Thành viên 1 — Lead Full-Stack Developer: Chịu trách nhiệm kiến trúc
    Backend, hệ thống Authentication, Security Hardening và API Design.

  • Thành viên 2 — Frontend Developer: Phát triển giao diện người dùng
    với React, xây dựng Component System, thiết kế UI/UX responsive.

  • Thành viên 3 — Database & QA Specialist: Thiết kế ERD/DFD, viết
    Migration, Seeder và bộ kiểm thử tự động (Jest Test Suite).

  • Thành viên 4 — DevOps & Integration: Triển khai Docker, CI/CD lên
    Railway, tích hợp Google OAuth, Cloudinary, Cloudflare Turnstile."


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 3: TỔNG QUAN DỰ ÁN — VẤN ĐỀ VÀ GIẢI PHÁP                   │
└─────────────────────────────────────────────────────────────────────┘

"Vấn đề đặt ra: Các trường học và tổ chức giáo dục hiện nay đều cần thu
thập ý kiến sinh viên về nhận thức môi trường, nhưng thường làm thủ công
bằng form giấy hoặc Google Form — thiếu hệ thống quản lý tập trung,
không có cơ chế khuyến khích tham gia, và không theo dõi được hoạt động
thực tế của sinh viên.

EcoSurvey giải quyết bằng một nền tảng web toàn diện với 4 trụ cột chính:

  1. Survey System: Hệ thống khảo sát trực tuyến với đa dạng loại câu hỏi
     (trắc nghiệm, tự luận, thang đánh giá, có/không), hỗ trợ phân quyền
     đối tượng khảo sát theo vai trò (Student, Staff, All).

  2. Participation Tracking: Module nộp minh chứng hoạt động môi trường —
     sinh viên upload hình ảnh, mô tả hoạt động thực tế, Admin duyệt và
     cấp điểm rèn luyện.

  3. Gamification Engine: Hệ thống Eco-Points, Badges và Leaderboard — mỗi
     lần hoàn thành khảo sát hoặc được duyệt minh chứng đều được cộng điểm,
     khi đạt mốc sẽ tự động nhận huy hiệu.

  4. AI Integration: Tích hợp Gemini AI qua OpenRouter API cho hai tính năng:
     FAQ Chatbot trả lời tự động câu hỏi thường gặp, và Auto-Summarize tóm
     tắt báo cáo hoạt động giúp Admin duyệt nhanh hơn."


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 4: TECH STACK — LỰA CHỌN CÔNG NGHỆ & LÝ DO                  │
└─────────────────────────────────────────────────────────────────────┘

"Về lựa chọn công nghệ, nhóm em xin giải thích lý do cho từng thành phần:

── FRONTEND ──────────────────────────────────────────────────────────

  • React.js: Được chọn vì kiến trúc Component-Based, cho phép tái sử dụng
    UI components (FeatureCard, SurveyCard, StepCard...) trên nhiều màn hình.
    Virtual DOM (Document Object Model) đảm bảo hiệu năng khi render 
    danh sách khảo sát lớn.

  • Vite: Build tool thế hệ mới, Hot Module Replacement (HMR) gần như tức
    thì — tăng tốc trải nghiệm lập trình gấp nhiều lần so với Webpack.

  • TailwindCSS: Utility-first CSS framework, cho phép xây dựng giao diện
    responsive nhanh chóng mà không cần viết file CSS riêng, đảm bảo thiết
    kế nhất quán giữa các thành viên.

── BACKEND ───────────────────────────────────────────────────────────

  • Node.js + Express.js: Nhóm chọn kiến trúc RESTful API với Express vì
    hệ sinh thái middleware phong phú (Helmet, CORS, express-rate-limit),
    phù hợp với mô hình I/O-bound của ứng dụng khảo sát (nhiều request
    đọc/ghi nhẹ, ít tính toán nặng).

  • Socket.IO: Cần real-time notification — khi Admin duyệt minh chứng hoặc
    có khảo sát mới, user nhận thông báo tức thì mà không cần F5 lại trang.

── DATABASE ──────────────────────────────────────────────────────────

  • MySQL: Đây là lựa chọn có chủ đích. Hệ thống EcoSurvey đòi hỏi tính
    TOÀN VẸN DỮ LIỆU cao — dữ liệu khảo sát, điểm rèn luyện, minh chứng
    hoạt động đều có quan hệ chặt chẽ và không được phép mất mát hay bất
    nhất quán.

    Database được thiết kế theo chuẩn hóa 3NF với 14 bảng dữ liệu, sử dụng
    Foreign Key Constraints quản lý chặt chẽ các quan hệ giữa các bảng.
    Ví dụ: Một SurveyAnswer PHẢI thuộc về đúng một SurveyResponse, và
    SurveyResponse PHẢI thuộc về một Survey hợp lệ — nếu vi phạm, database
    sẽ từ chối thao tác ngay lập tức.

    ★ NẾU THẦY HỎI: 'Tại sao không dùng NoSQL (MongoDB)?'

    → Trả lời: NoSQL phù hợp với dữ liệu phi cấu trúc, schema linh hoạt,
    scale-out ngang. Nhưng EcoSurvey cần ACID transactions (ví dụ: khi cộng
    điểm cho user, phải đảm bảo cả PointLog VÀ cập nhật trạng thái
    Participation đều thành công hoặc đều rollback). MongoDB không hỗ trợ
    multi-document transactions mạnh mẽ bằng MySQL. Thêm nữa, dữ liệu
    khảo sát có cấu trúc rõ ràng (Survey → Question → Answer), quan hệ
    nhiều tầng, JOIN phức tạp — đây chính là thế mạnh của Relational Database.

  • Sequelize ORM: Giúp định nghĩa Model bằng JavaScript (code-first),
    không cần viết SQL thủ công cho từng bảng. Hỗ trợ sync() đồng bộ schema
    (đã loại bỏ alter: true để ngăn việc tự động sinh trùng lặp các chỉ mục
    UNIQUE/INDEX gây vượt quá hạn ngạch 64 keys của MySQL "Too many keys specified").
    Đồng thời hỗ trợ Migration và Seeder để quản lý phiên bản database trong team.

── THIRD-PARTY SERVICES ──────────────────────────────────────────────

  • Google OAuth 2.0: Single Sign-On cho phép user đăng nhập bằng tài khoản
    Google — tiện lợi trong môi trường học đường (Gmail).

  • Cloudflare Turnstile: CAPTCHA thế hệ mới, bảo vệ form đăng ký/đăng nhập
    khỏi bot spam mà không gây phiền hà cho user (privacy-preserving).

  • Cloudinary: Cloud storage cho file upload — ảnh minh chứng và avatar
    được lưu trữ trên CDN, giảm tải cho server chính.

  • OpenRouter + Gemini AI: Tích hợp AI để trả lời FAQ tự động và tóm tắt
    báo cáo hoạt động — giảm tải công việc cho Admin.

  • ExcelJS & PDFKit: Đọc/ghi file Excel (.xlsx) xử lý Import tài khoản hàng loạt & Export khảo sát; render file PDF (PDFKit + font Roboto tiếng Việt) xuất báo cáo hoạt động."


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 5: DATA FLOW DIAGRAM (DFD) — LUỒNG DỮ LIỆU                  │
└─────────────────────────────────────────────────────────────────────┘

"Về luồng dữ liệu tổng quan, hệ thống hoạt động theo 4 luồng chính:

  Luồng 1 — Authentication Flow:
  User gửi thông tin đăng ký/đăng nhập → Backend xác thực qua CAPTCHA →
  Kiểm tra credentials → Tạo cặp Access Token (JWT 15 phút) + Refresh
  Token (Cookie 30 ngày) → Trả về cho Frontend.
  Hoặc: User chọn Đăng nhập Google → Passport.js xử lý OAuth flow →
  Google trả profile → Backend match/tạo user → Redirect kèm token.

  Luồng 2 — Survey Flow:
  Admin tạo Survey (Draft) → Publish → Cron job tự động đóng khi hết hạn.
  Student/Staff mở Survey Board → Chọn khảo sát → Trả lời → Submit →
  Backend lưu SurveyResponse + SurveyAnswers → Cộng Eco-Points → Gửi
  Notification real-time qua Socket.IO.

  Luồng 3 — Participation Flow:
  User nộp minh chứng hoạt động môi trường (mô tả + upload ảnh) →
  AI tự động tóm tắt (ai_summary) → Admin xem và duyệt/từ chối →
  Nếu duyệt: Cộng điểm + gửi thông báo. Nếu từ chối: Gửi lý do.

  Luồng 4 — Gamification Flow:
  Mỗi hành động cộng điểm → PointLog ghi lại → Badge Service kiểm tra
  điều kiện (SURVEY_COUNT, PARTICIPATION_COUNT, TOTAL_POINTS,
  LEADERBOARD_RANK...) → Tự động cấp Badge → Cron job chạy mỗi giờ
  kiểm tra Top 10 Leaderboard Badge."


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 6: ERD — SƠ ĐỒ QUAN HỆ THỰC THỂ                             │
└─────────────────────────────────────────────────────────────────────┘

"Database gồm 14 bảng, thiết kế theo chuẩn hóa 3NF. Em xin trình bày
các nhóm quan hệ chính:

  ── Nhóm Authentication (3 bảng) ──────────────────────────────────
  • users: Bảng trung tâm, lưu thông tin cá nhân, role (Student/Staff/Admin),
    status (Pending/Approved/Rejected/Locked/Deactivated), hỗ trợ cả local auth
    (password_hash) và Google OAuth (google_id).
  • refresh_tokens: Lưu SHA-256 hash của refresh token, liên kết user_id,
    có cơ chế revoked flag + expires_at.
  • 1 User → N RefreshTokens (hỗ trợ đăng nhập multi-device).

  ── Nhóm Survey (4 bảng) ─────────────────────────────────────────
  • surveys: Tiêu đề, mô tả, target_role, start_date, end_date, status.
  • questions: Thuộc 1 survey, có type (multiple_choice, text, rating, yes_no).
  • survey_responses: Ghi nhận lượt trả lời của user cho survey.
  • survey_answers: Câu trả lời cụ thể cho từng question trong response.
  • Quan hệ: Survey (1) → (N) Question → (N) SurveyAnswer.
              Survey (1) → (N) SurveyResponse → (N) SurveyAnswer.

  ── Nhóm Participation (2 bảng) ──────────────────────────────────
  • participations: Minh chứng hoạt động (event_name, location, description,
    status, ai_summary, reviewed_by).
  • participation_files: File ảnh đính kèm (Cloudinary URL).
  • 1 Participation → N ParticipationFiles.

  ── Nhóm Gamification (3 bảng) ───────────────────────────────────
  • point_logs: Ghi lại mọi giao dịch điểm (action_type: Survey_Completion,
    Event_Report, Bonus, Deduction). Append-only — không sửa/xóa.
  • badges: Định nghĩa huy hiệu (condition_type + condition_value).
  • user_badges: Bảng trung gian N-N giữa User và Badge.

  ── Nhóm Support (2 bảng) ────────────────────────────────────────
  • notifications: Thông báo real-time cho từng user.
  • faqs: Câu hỏi thường gặp, làm ngữ liệu cho AI Chatbot.

  Tại sao point_logs dùng mô hình Append-only (chỉ INSERT, không UPDATE/DELETE)?
  → Để đảm bảo tính minh bạch và kiểm toán: mọi biến động điểm đều có
  dấu vết, không ai có thể sửa lịch sử điểm, Admin có thể truy vết
  từng giao dịch khi cần."


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 7: SYSTEM ARCHITECTURE — KIẾN TRÚC HỆ THỐNG                  │
└─────────────────────────────────────────────────────────────────────┘

"Kiến trúc hệ thống theo mô hình Client-Server tách biệt:

  ┌──────────────┐     HTTPS/REST API      ┌──────────────────────┐
  │   Frontend   │ ◄──────────────────────► │      Backend         │
  │  React + Vite│     + Socket.IO          │  Express + Socket.IO │
  │  Port 3000   │                          │  Port 5000           │
  └──────────────┘                          └──────────┬───────────┘
                                                       │
                                            ┌──────────▼───────────┐
                                            │     MySQL Database   │
                                            │     14 Tables (3NF)  │
                                            └──────────────────────┘
                                                       │
                                            ┌──────────▼───────────┐
                                            │   External Services  │
                                            │  • Cloudinary (CDN)  │
                                            │  • Google OAuth 2.0  │
                                            │  • Cloudflare CAPTCHA│
                                            │  • OpenRouter AI     │
                                            └──────────────────────┘

  Backend được tổ chức theo kiến trúc MVC:
  • Routes → Controllers → Services → Models (Sequelize) → MySQL.
  • Middleware pipeline: Helmet → CORS → Rate Limiter → Body Parser →
    Cookie Parser → Passport → Route Handlers → Error Handler."


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 8: SECURITY — BẢO MẬT HỆ THỐNG                               │
└─────────────────────────────────────────────────────────────────────┘

"Bảo mật là một trong những trọng tâm đầu tư lớn nhất của dự án. Hệ thống
áp dụng mô hình Defense-in-Depth (bảo vệ nhiều lớp):

  Lớp 1 — Transport Layer:
  • HSTS (2 năm) ép trình duyệt luôn dùng HTTPS.
  • Helmet CSP (Content Security Policy) chặn XSS, clickjacking, plugin injection.

  Lớp 2 — Authentication & Multi-Domain Session:
  • JWT Access Token (15 phút, lưu localStorage) + Refresh Token Cookie (30 ngày, SHA-256 hashed).
  • Khôi phục phiên đăng nhập thông minh bằng Bearer Token từ localStorage 
    ở AuthContext (duy trì trạng thái đăng nhập ổn định khi reload trang 
    ngay cả khi browser chặn Cookie 3rd-party cross-domain giữa Vercel & Railway).
  • Google OAuth 2.0 qua Passport.js tích hợp mã hóa `state` (Base64URL) 
    để điều hướng động chính xác về từng client domain Vercel (multi-domain client support).
  • Cloudflare Turnstile CAPTCHA bảo vệ form đăng ký/đăng nhập.
  • Email Service sử dụng Nodemailer tối ưu tự động cho `service: 'gmail'`, 
    tự động làm sạch App Password (strip spaces) và đồng bộ Header 'From' trùng khớp 
    SMTP_USER để chống bị đưa vào Spam/Junk.

  Lớp 3 — Authorization Layer:
  • Role-Based Access Control (RBAC): Student, Staff, Admin.
  • ProtectedRoute component ở Frontend kiểm tra role trước khi render page.
  • Backend middleware kiểm tra req.user.role cho mỗi API endpoint.

  Lớp 4 — Rate Limiting & DoS Protection:
  • generalLimiter: 300 req/phút mount toàn cục trên Express.
  • loginLimiter: 10 req/phút chống brute-force.
  • aiLimiter: 10 req/phút kiểm soát chi phí AI API.
  • surveySubmitLimiter: 5 req/phút chống spam nộp bài.
  • Pagination cap: Giới hạn limit tối đa 100 bản ghi/request chống
    Memory Exhaustion DoS.

  Lớp 5 — Production Safeguards & DB Integrity:
  • Fail-Fast startup: Server dừng ngay nếu thiếu JWT_SECRET, DB credentials,
    OAuth keys hoặc CAPTCHA secret key ở production.
  • CAPTCHA Fail-Closed: Trả HTTP 500 nếu thiếu TURNSTILE_SECRET_KEY ở
    production thay vì bypass.
  • Loại bỏ `sequelize.sync({ alter: true })` chuyển sang `sequelize.sync()` để bảo vệ DB khỏi lỗi cạn kiệt index key (MySQL max 64 keys limit).
  • Loại bỏ hoàn toàn việc đọc JWT từ URL query string để tránh rò rỉ
    token qua Server Logs, Browser History, và HTTP Referer Header."


════════════════════════════════════════════════════════════════════════
  BẮT ĐẦU DEMO SẢN PHẨM — MÀN HÌNH & KIẾN TRÚC KỸ THUẬT NỀN TẢNG
════════════════════════════════════════════════════════════════════════


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 1: LANDING PAGE — TRANG CHỦ & LỚP BẢO VỆ REQUEST CLIENT    │
└─────────────────────────────────────────────────────────────────────┘

"Kính thưa thầy cô, đây là trang đầu tiên người dùng tiếp cận hệ thống, 
thiết kế theo phong cách Neo-Brutalism hiện đại với Đa ngôn ngữ (i18n) 
và Dark/Light theme. 

Về mặt Kỹ thuật & Bảo mật đằng sau mọi Request:
  • Khi Client vừa truy cập, mọi HTTP request gửi lên Backend đều phải đi 
    qua Express Security Middleware Pipeline.
  • Lớp bảo mật Helmet CSP (Content Security Policy) lập tức đính kèm 
    Header `Content-Security-Policy` khắt khe:
    - `defaultSrc: ["'self'"]`: Chỉ tải tài nguyên từ origin an toàn.
    - `scriptSrc` & `styleSrc`: Whitelist mã nguồn nội bộ và Google Fonts.
    - `imgSrc`: Cho phép nạp ảnh local, Blob/Base64 và Cloudinary CDN.
    - `objectSrc` & `frameAncestors`: Cấm tuyệt đối Flash và Iframe để 
      chống triệt để tấn công Clickjacking (UI Redress).
  • Bên cạnh đó, HSTS ép trình duyệt dùng HTTPS (maxAge 2 năm) và 
    `generalLimiter` (300 req/phút) bảo vệ toàn server khỏi tấn công DoS."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 2: ĐĂNG KÝ, ĐĂNG NHẬP, CƠ CHẾ JWT & SESSION SECURITY       │
└─────────────────────────────────────────────────────────────────────┘

"Màn hình Đăng ký / Đăng nhập hỗ trợ cả Form truyền thống và Google OAuth 2.0.

Về Cơ chế Mã hóa & Bảo mật Phiên làm việc (Auth & JWT):
  • Đăng ký (Register): Bắt buộc vượt qua xác thực Cloudflare Turnstile 
    CAPTCHA chống Bot spam. Mật khẩu được băm `bcrypt` (`SALT_ROUNDS = 10`). 
    Tài khoản vừa tạo nhận `status = 'Pending'`. Backend tự động gửi Email 
    xác nhận qua Nodemailer Gmail SMTP (`smtp.gmail.com:587` có Fallback 
    sang cổng `465 SSL`), đồng thời phát event Socket.IO real-time báo Admin.

  • Đăng nhập (Login): Backend kiểm tra trạng thái CSDL. Nếu status là 
    `Pending`/`Rejected`/`Locked` → Trả HTTP 403 từ chối ngay. 
    Nếu `Approved` → Cấp ngay cặp JWT Tokens:
    1. Access Token: Chuỗi JWT ngắn hạn (15 phút), chứa payload `{ user_id, 
       role, full_name }`, mã hóa bằng `JWT_SECRET`. Trả về trong JSON body 
       → Frontend lưu vào `localStorage`.
    2. Refresh Token: Chuỗi ngẫu nhiên `crypto.randomBytes(64)` (30 ngày), 
       băm SHA-256 (`token_hash`) lưu trong CSDL MySQL. Trả về qua 
       HTTP-Only Cookie (`SameSite=None`, `Secure`) → Ngăn chặn hoàn toàn 
       tấn công XSS đọc trộm Cookie và chống CSRF.

  • Google OAuth 2.0: Sử dụng Passport.js mã hóa tham số `state` (Base64URL) 
    chứa origin client gốc để điều hướng động chính xác về Vercel Frontend."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 3: DASHBOARD CÁ NHÂN & CƠ CHẾ KHÔI PHỤC PHIÊN LÀM VIỆC    │
└─────────────────────────────────────────────────────────────────────┘

"Trang Dashboard cá nhân tổng hợp số liệu khảo sát, Eco-Points tích lũy, 
thứ hạng Leaderboard và 5 giao dịch điểm gần nhất.

Về Vận hành API & Khôi phục Phiên làm việc (Auth Session Fallback):
  • Mọi request lấy thông tin cá nhân (`/api/auth/me`) đính kèm Access Token 
    vào Header `Authorization: Bearer <access_token>`.
  • Auth Session Fallback: Khi reload (F5) trang, nếu trình duyệt (như Safari) 
    chặn 3rd-party cookie cross-domain giữa Vercel & Railway → `AuthContext` 
    tự động lấy Access Token từ `localStorage` để duy trì phiên đăng nhập 
    mà người dùng không bị văng ra ngoài.
  • Axios Interceptor: Khi Access Token 15 phút hết hạn → Tự động bắt lỗi 401 
    và âm thầm gọi `/api/auth/refresh` lấy Access Token mới qua Refresh Token 
    Cookie mà không làm gián đoạn trải nghiệm."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 4: SURVEY BOARD — DANH SÁCH KHẢO SÁT & PHÂN QUYỀN RBAC    │
└─────────────────────────────────────────────────────────────────────┘

"Trang Survey Board hiển thị danh sách khảo sát đang phát hành (Published).

Về Phân quyền & Giới hạn Tải (RBAC & Memory Protection):
  • Backend kiểm tra `req.user.role` để thực thi Role-Based Access Control (RBAC). 
    Sinh viên chỉ nhìn thấy khảo sát dành cho `Student` và `All`, nhân viên chỉ 
    thấy khảo sát cho `Staff` và `All`.
  • Để chống tấn công cạn kiệt bộ nhớ (Memory Exhaustion DoS), helper 
    `pagination.js` cap cứng giới hạn `limit` tối đa 100 bản ghi/request."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 5: SURVEY DETAIL — LÀM KHẢO SÁT & CHỐNG SPAM GIAN LẬN      │
└─────────────────────────────────────────────────────────────────────┘

"Giao diện làm bài hỗ trợ 4 loại câu hỏi linh hoạt (Multiple Choice, Text, 
Rating, Yes/No).

Về Xử lý Giao dịch & Chống gian lận (ACID Transaction):
  • `surveySubmitLimiter`: Giới hạn 5 lần submit/phút chống spam.
  • Khi người dùng bấm Nộp bài, Backend mở một MySQL Database Transaction:
    1. Kiểm tra CSDL đảm bảo người dùng chưa từng làm khảo sát này.
    2. Ghi nhận `SurveyResponse` và các `SurveyAnswer` tương ứng.
    3. Thêm nhật ký cộng điểm vào bảng `point_logs` (`action_type = 
       'Survey_Completion'`). Bảng này thiết kế Append-only (chỉ INSERT) 
       đảm bảo minh bạch tuyệt đối.
    4. Kích hoạt `BadgeService` tự động quét và cấp Huy hiệu mới.
    5. Phát thông báo real-time qua Socket.IO tới thiết bị người dùng."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 6: PARTICIPATION — MINH CHỨNG HOẠT ĐỘNG & AI SUMMARY     │
└─────────────────────────────────────────────────────────────────────┘

"Màn hình cho phép sinh viên nộp minh chứng hoạt động bảo vệ môi trường 
thực tế (hình ảnh + mô tả).

Về Bảo mật File Upload & Tích hợp AI (Cloudinary & Gemini AI):
  • File Upload Security: Middleware `Multer` kiểm tra nghiêm ngặt CẢ 
    đuôi file (`.jpg`, `.png`) VÀ MIME type (`image/jpeg`, `image/png`) 
    để chống tải lên file thực thi độc hại (`.php`, `.exe`).
  • Lưu trữ CDN: File ảnh được đẩy trực tiếp lên **Cloudinary CDN** qua HTTPS, 
    chỉ lưu URL mã hóa vào bảng `participation_files`.
  • AI Gemini 2.5 Flash & Fallback: Ngay khi nộp, Backend tự động gọi AI 
    Gemini qua OpenRouter để tóm tắt nội dung báo cáo thành 2-3 câu 
    (`ai_summary`). Nếu AI API bị quá tải hoặc mất mạng → Tự động kích hoạt 
    **Local Extractive Fallback** (`mockSummary`) trích 2 câu đầu tiên, 
    đảm bảo hệ thống KHÔNG BAO GIỜ BỊ SẬP."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 7: LEADERBOARD — BẢNG XẾP HẠNG & OPTIMIZED RAW SQL        │
└─────────────────────────────────────────────────────────────────────┘

"Leaderboard vinh danh các cá nhân có tổng Eco-Points cao nhất toàn trường.

Về Tối ưu Truy vấn & Tự động hóa (Raw SQL & Cron Job):
  • Để tính toán thứ hạng chính xác từ hàng vạn giao dịch điểm, Backend 
    không qua Sequelize ORM mà chạy **Optimized Raw SQL** với câu lệnh 
    `SUM(point_logs.points)` kết hợp window function `DENSE_RANK() OVER (...)`.
  • Hệ thống tích hợp `node-cron` chạy ngầm mỗi giờ tự động rà soát 
    Top 10 và trao Huy hiệu Bảng xếp hạng (`LEADERBOARD_RANK`)."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 8: PROFILE — TRANG CÁ NHÂN & ĐỔI MẬT KHẨU BẢO MẬT         │
└─────────────────────────────────────────────────────────────────────┘

"Trang Profile quản lý thông tin cá nhân, avatar, danh hiệu và cài đặt giao diện.

Về Bảo mật Mật khẩu & Lưu trữ Trạng thái:
  • Trường `ui_theme` (Light/Dark) được lưu trực tiếp vào CSDL MySQL để 
    đồng bộ trạng thái giao diện nhất quán trên mọi thiết bị.
  • Đổi mật khẩu: Bắt buộc xác thực lại mật khẩu cũ qua `bcrypt.compare()`. 
    Mật khẩu mới được băm `bcrypt` an toàn trước khi lưu vào CSDL."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 9: ADMIN DASHBOARD — TỔNG QUAN & EXPORT PDF/EXCEL        │
└─────────────────────────────────────────────────────────────────────┘

"Admin Dashboard cung cấp cái nhìn toàn cảnh về chỉ số hệ thống, biểu đồ 
phân bổ và lượt phản hồi 7 ngày gần nhất.

Về Tối ưu API & Xuất Báo cáo (Parallel Query & PDFKit/ExcelJS):
  • Tối ưu API: Backend dùng `Promise.all()` thực thi 6 truy vấn SQL song song, 
    giảm 80% thời gian phản hồi cho API `/api/admin/dashboard`.
  • Export PDF Báo cáo (`PDFKit`): Xuất báo cáo tổng quan ra file PDF chuẩn. 
    Hệ thống tích hợp font Roboto tiếng Việt Unicode, tự động căn lề `x = 50` 
    và chuẩn hóa chiều rộng bảng `495px` tạo file PDF cân đối, chuyên nghiệp.
  • Export Excel (`ExcelJS`): Xuất toàn bộ dữ liệu khảo sát ra file `.xlsx` 
    với định dạng cột màu sắc rõ ràng."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 10: USER MANAGEMENT — QUẢN LÝ TÀI KHOẢN & IMPORT EXCEL   │
└─────────────────────────────────────────────────────────────────────┘

"Màn hình cho phép Admin kiểm soát toàn bộ tài khoản người dùng trong trường.

Về Quản lý Trạng thái & Import Hàng loạt:
  • Hệ thống Trạng thái (`status`): Hỗ trợ `Approved` (cho phép đăng nhập), 
    `Rejected` (từ chối đơn), và `Locked` (khóa tài khoản). Badge `Locked` 
    được thiết kế màu Amber Neo-Brutalist nổi bật (`badge-locked`).
  • Thông báo Đa kênh: Khi duyệt/từ chối/khóa, Backend lập tức gửi Email 
    kết quả kèm lý do qua Gmail SMTP, đồng thời bắn thông báo real-time 
    qua Socket.IO tới người dùng.
  • Import Excel hàng loạt (`ExcelJS`): Admin tải lên file `.xlsx` danh sách 
    sinh viên/nhân viên → Backend tự động đọc buffer, kiểm tra trùng lặp email, 
    băm mật khẩu `bcrypt` và cấp trạng thái `Approved` ngay lập tức."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 11: SURVEY MANAGEMENT — CẤU TRÚC KHẢO SÁT & CRON CLOSE    │
└─────────────────────────────────────────────────────────────────────┘

"Admin quản lý trọn vẹn vòng đời bài khảo sát từ Soạn thảo (Draft) → 
Phát hành (Published) → Đóng khảo sát (Closed).

Về Tự động đóng Khảo sát (Auto-Close Cron Job):
  • Hệ thống tích hợp Cron Job chạy ngầm mỗi giờ: Quét các khảo sát có 
    `end_date < NOW()` và tự động cập nhật `status = 'Closed'`, ngăn chặn 
    người dùng làm bài quá hạn mà không cần Admin thao tác thủ công."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 12: PARTICIPATION REVIEW — DUYỆT MINH CHỨNG & EXPORT PDF │
└─────────────────────────────────────────────────────────────────────┘

"Admin xem xét và duyệt các minh chứng hoạt động môi trường do sinh viên gửi lên.

Về Hỗ trợ AI & Xuất Báo cáo Bằng chứng (AI & PDFKit):
  • Đọc báo cáo nhanh nhờ `ai_summary` do Gemini AI tự động tóm tắt.
  • Khi Admin bấm Approve → Hệ thống mở Transaction cộng 50 Eco-Points, 
    kiểm tra cấp Huy hiệu, gửi Email mừng và bắn Socket.IO notification.
  • Export PDF Minh chứng (`PDFKit`): Tự động tổng hợp danh sách các hoạt động 
    đã `Approved` xuất thành file PDF chính thức kèm thông tin sự kiện, địa điểm, 
    số người tham gia, tóm tắt AI và chữ ký duyệt của Admin."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 13: FAQ MANAGEMENT & AI CHATBOT (HYBRID AI FALLBACK)      │
└─────────────────────────────────────────────────────────────────────┘

"Màn hình quản lý bộ câu hỏi thường gặp FAQ và trợ lý AI Chatbot.

Về Kiến trúc RAG & AI Fallback (OpenRouter & Local Matching):
  • Trợ lý AI Gemini 2.5 Flash sử dụng toàn bộ CSDL `faqs` làm ngữ liệu 
    chính (Context RAG), đảm bảo câu trả lời luôn chính xác với quy định 
    nhà trường, không bị ảo giác (Hallucination).
  • Hybrid AI Fallback: Nếu API AI mất kết nối hoặc hết hạn ngạch, 
    hệ thống tự động kích hoạt `mockFAQAnswer()` tách từ khóa và so khớp 
    truy vấn MySQL FAQ để trả về câu trả lời phù hợp nhất mà không bị ngắt quãng."


┌─────────────────────────────────────────────────────────────────────┐
│  MÀN HÌNH 14: REAL-TIME NOTIFICATION (SOCKET.IO WEBSOCKET SYSTEM)   │
└─────────────────────────────────────────────────────────────────────┘

"Hệ thống thông báo tức thì hiển thị trên Navbar của tất cả màn hình.

Về Kiến trúc WebSocket & Multi-Tab Synchronization:
  • Socket.IO Handshake Authentication: Khi kết nối WebSocket, Client bắt buộc 
    truyền JWT Access Token trong handshake. Backend giải mã JWT xác thực 
    mới cho phép kết nối và join vào room cá nhân `user_{userId}`.
  • Đẩy thông báo tức thì: Khi có sự kiện (Duyệt bài, Cấp huy hiệu, Khảo sát mới), 
    Backend emit event tới room `user_{userId}` → Tất cả các tab trình duyệt 
    đang mở của người dùng đó đều nhận được popup thông báo đồng thời."



┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 9: TESTING & QUALITY ASSURANCE                                │
└─────────────────────────────────────────────────────────────────────┘

"Nhóm áp dụng bộ kiểm thử tự động bằng Jest:

  • 5 bộ test suite: auth, admin, survey, leaderboard, cors.
  • 20 test cases: Kiểm tra đăng ký, đăng nhập, CRUD survey,
    multi-origin CORS, rate limiting.
  • Kết quả: 20/20 PASSED, 100% pass rate.
  • CI có thể chạy npm test trước mỗi lần deploy để đảm bảo không
    có regression."


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 10: DEPLOYMENT — TRIỂN KHAI                                   │
└─────────────────────────────────────────────────────────────────────┘

"Hệ thống được triển khai trên Railway với Docker:

  • Backend: Node.js container + MySQL database trên Railway.
  • Frontend: Build static → deploy lên hosting.
  • Docker Compose cho môi trường local development và production.
  • Environment Variables được quản lý tập trung trên Railway Dashboard.
  • Health Check endpoint: GET /api/health → { status: 'OK' }."

  Do Railway Free/Trial/Hobby plans
  chặn toàn bộ các cổng SMTP TCP đầu ra (25, 465, 587)nên là chức năng smtp 
  không hoạt động được trên môi trường deploy


┌─────────────────────────────────────────────────────────────────────┐
│  SLIDE 11: KẾT LUẬN & HƯỚNG PHÁT TRIỂN                              │
└─────────────────────────────────────────────────────────────────────┘

"Tổng kết, nhóm đã xây dựng thành công một nền tảng web full-stack
hoàn chỉnh với:

  ✓ Hệ thống khảo sát linh hoạt với 4 loại câu hỏi.
  ✓ Module nộp minh chứng hoạt động môi trường kèm AI tóm tắt.
  ✓ Gamification: Eco-Points, Badges (6 loại điều kiện), Leaderboard.
  ✓ Authentication đa phương thức: Local + Google OAuth + Email Verification.
  ✓ Bảo mật nhiều lớp: JWT, CAPTCHA, Rate Limiting, CORS, Helmet, HSTS.
  ✓ Real-time Notification qua Socket.IO.
  ✓ AI Chatbot trả lời FAQ tự động.
  ✓ Đa ngôn ngữ (i18n: Tiếng Việt / English).
  ✓ Dark/Light Theme.
  ✓ Automated Test Suite: 20/20 passed.

  Hướng phát triển tương lai:
  • Ứng dụng di động (React Native) để user nộp minh chứng ngoài hiện trường.
  • AI predictive analytics: Phân tích xu hướng nhận thức môi trường theo
    thời gian từ dữ liệu khảo sát.
  • Mở rộng báo cáo: Export kết quả khảo sát ra Excel/PDF.
  • Tích hợp CAS/LDAP cho hệ thống trường học lớn.

  Em xin cảm ơn quý thầy cô đã lắng nghe. Nhóm sẵn sàng trả lời câu hỏi ạ."


════════════════════════════════════════════════════════════════════════
  PHỤ LỤC: DỰ ĐOÁN CÂU HỎI PHẢN BIỆN & CÁCH TRẢ LỜI
════════════════════════════════════════════════════════════════════════


Q1: "Tại sao không dùng MongoDB (NoSQL) thay vì MySQL?"
────────────────────────────────────────────────────────
→ "Dạ thưa thầy, hệ thống EcoSurvey có dữ liệu quan hệ chặt chẽ — một
   Survey chứa nhiều Question, mỗi Question có nhiều Answer, mỗi Answer
   thuộc một Response. Chuỗi quan hệ này cần được ràng buộc bằng Foreign
   Key để đảm bảo toàn vẹn.

   Ngoài ra, khi cộng điểm cho user, hệ thống cần ACID transaction —
   phải đảm bảo cả việc tạo PointLog VÀ cập nhật trạng thái Participation
   đều thành công hoặc đều rollback. MySQL hỗ trợ transaction gốc mạnh mẽ,
   trong khi MongoDB chỉ hỗ trợ multi-document transaction từ bản 4.0 và
   hiệu năng kém hơn đáng kể.

   Cuối cùng, dữ liệu khảo sát có cấu trúc rõ ràng, không cần schema
   linh hoạt — đây chính là thế mạnh của Relational Database."


Q2: "Sequelize ORM có chậm hơn viết raw SQL không?"
────────────────────────────────────────────────────
→ "Dạ đúng là Sequelize có overhead so với raw SQL — mỗi query phải qua
   lớp abstraction để build SQL. Tuy nhiên overhead này rất nhỏ (vài ms)
   và được đánh đổi bằng:
   - Tốc độ phát triển nhanh hơn nhiều (code-first, auto-sync schema).
   - Chống SQL Injection tự động qua parameterized queries.
   - Dễ maintain khi team nhiều người.
   Với những query phức tạp (như tính rank Leaderboard), em vẫn dùng
   sequelize.query() viết raw SQL trực tiếp để tối ưu."


Q3: "Tại sao dùng localStorage cho Access Token mà không dùng Cookie?"
──────────────────────────────────────────────────────────────────────
→ "Access Token cần được gửi trong Authorization Header cho mọi API request.
   Nếu lưu trong Cookie, browser sẽ tự động gửi kèm → dễ bị tấn công CSRF.
   localStorage cho phép Frontend chủ động kiểm soát khi nào gửi token.
   Để giảm thiểu rủi ro XSS, Access Token có thời hạn rất ngắn (15 phút)
   và hệ thống có Helmet CSP chặn inline script injection."


Q4: "Socket.IO có scale được khi có nhiều server không?"
────────────────────────────────────────────────────────
→ "Hiện tại với single server, Socket.IO hoạt động tốt vì lưu danh sách
   socket connections trong memory (Map). Nếu cần scale ra nhiều server,
   em sẽ dùng @socket.io/redis-adapter — Redis pub/sub làm message broker
   giữa các server instances, đảm bảo event được broadcast tới đúng user
   dù họ đang kết nối ở server nào."


Q5: "Nếu AI API (OpenRouter) bị lỗi thì hệ thống xử lý thế nào?"
──────────────────────────────────────────────────────────────────
→ "Hệ thống có cơ chế Graceful Degradation. Trong aiService.js, nếu
   OPENROUTER_API_KEY không được cấu hình hoặc API call thất bại, hệ thống
   tự động fallback sang mock functions — trả về câu trả lời cơ bản bằng
   cách matching keywords từ FAQ database. Tính năng AI bị giảm chất lượng
   nhưng hệ thống KHÔNG bị crash hay từ chối phục vụ."


Q6: "Điểm (Eco-Points) có thể bị gian lận không?"
──────────────────────────────────────────────────
→ "Nhóm em đã áp dụng nhiều biện pháp chống gian lận:
   - surveySubmitLimiter: Giới hạn 5 lần nộp/phút.
   - Backend kiểm tra user đã nộp survey chưa trước khi cho phép submit.
   - Minh chứng hoạt động phải qua Admin duyệt thủ công mới được cộng điểm.
   - point_logs là append-only — không ai sửa/xóa lịch sử điểm được.
   - Tài khoản phải được Admin Approve mới được sử dụng → chống tạo
     nhiều tài khoản fake để farm điểm."


Q7: "Hệ thống xử lý user đăng nhập trên nhiều thiết bị thế nào?"
────────────────────────────────────────────────────────────────
→ "Hệ thống hỗ trợ multi-device login. Mỗi lần đăng nhập tạo một
   Refresh Token mới, KHÔNG revoke token cũ. Mỗi thiết bị có cookie
   refreshToken riêng, tất cả đều hợp lệ song song. Nếu muốn chuyển
   sang single-device, chỉ cần revoke tất cả token cũ khi tạo token mới."


Q8: "Tại sao user đăng ký phải chờ Admin duyệt? Có phiền không?"
────────────────────────────────────────────────────────────────
→ "Đây là thiết kế có chủ đích cho môi trường giáo dục. Hệ thống cần
   đảm bảo chỉ sinh viên và nhân viên thật của trường mới được tham gia.
   Admin xác minh Mã sinh viên / Mã nhân viên trước khi Approve. Điều này
   ngăn chặn người ngoài tạo tài khoản giả để ảnh hưởng kết quả khảo sát.
   Ngoài ra, Email Verification cũng giúp xác nhận email hợp lệ."


Q9: "Hệ thống có hỗ trợ export dữ liệu không?"
──────────────────────────────────────────────
→ "Có ạ. Backend có module exportRoutes.js cho phép Admin export kết quả
   khảo sát. Hiện tại hỗ trợ export qua API, tương lai sẽ mở rộng thêm
   định dạng Excel và PDF."


Q10: "Cron job nếu bị lỗi thì sao? Có log không?"
──────────────────────────────────────────────────
→ "Cron job được bọc trong try/catch. Nếu lỗi, logger.error ghi lại
   chi tiết lỗi vào Winston log (console + file). Cron chạy mỗi giờ
   nên nếu 1 lần lỗi, lần chạy sau sẽ bắt lại các survey chưa được
   close. Không ảnh hưởng đến hoạt động chính của server."
